// Tailwind 4 through PostCSS. The reference uses @tailwindcss/vite; Next.js has
// no Vite, so the PostCSS plugin is the equivalent.
const config = {
  plugins: ["@tailwindcss/postcss"],
};

export default config;
