{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The interface is served from the application bundle over the tauri:// protocol, not from a remote origin \u2014 so the application's own commands need no explicit permission here. Compare the Rantai spike, which had to grant http://127.0.0.1:* because its interface was served over HTTP by openwork-server.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "deep-link:default",
    "opener:default",
    "dialog:default",
    "updater:default"
  ]
}
